# Merge Plan: E2-2 + E2-3 + E5-1 → `feat/exec-2026-08-02-phase0`

> Date: 2026-08-03 · Target tip: `591f924` (common ancestor of all 3 branches)
> Branches: E2-2 `feat/exec-2026-08-02-e22`@`5b91780` · E2-3 `feat/exec-2026-08-02-e23`@`7003fbf` · E5-1 `feat/exec-2026-08-02-e51`@`9422c96`

## 1. Merge order: E2-3 → E2-2 → E5-1 (adopted — recommended order confirmed)

Justification:

1. **E2-3 first (coupling)**: E2-3 rewrites `Services/ActiveSpeechRouter.cs` `Route(NPC, Farmer, string)` to delegate to the new `SpeechDisplayRouter.Route` (sentence-count-first display rules). E2-2's `Chat/ChatBarRouter.ProcessPendingReplies()` calls `ActiveSpeechRouter.Route(npc, Game1.player, reply.Speech)`. Signature verified identical across branches (public face preserved; XML doc states "public 面不变，EventHandlerInitializer 调用点无需改动"). Merging E2-3 first means every intermediate tree is consistent with final intent: chat-bar replies flow through the E2-3 display pipeline from the moment E2-2 lands.
2. **E2-3 also wires the display sinks** (`SpeechDisplayRouter.ApplyConfig/SetChatSink/SetBubbleSink` in `ServiceInitializer`) — pipeline fully functional before E2-2 consumes it.
3. **E2-2 second**: its only textual overlaps with E2-3 are `ModConfig.cs` + `EventHandlerInitializer.cs` — both verified disjoint regions (below); no ordering advantage there, so coupling wins.
4. **E5-1 last**: only overlaps E2-3 in `ServiceInitializer.cs` (verified disjoint: usings at base L25 vs L28; registrations at base L68 vs L214). Zero overlap with E2-2. Last position minimizes the tiny conflict surface.

## 2. Conflict hotspots + resolution (each verified against actual branch diffs, not assumed)

### H1. `src/ValleyAgent/Config/ModConfig.cs`
- E2-2: 5-property chat-routing block after `FallbackAIMixProbability` (base ~L195) + 4-line clamp block after `MaxConsecutiveIdleBeforeRelease` clamp (base ~L439).
- E2-3: `LongTextIntervalMs` after `ChatBubbleEnabled` (base ~L307) + clamp after `DialogueCooldownMs` clamp (base ~L424).
- **VERIFIED**: two property blocks and two clamp blocks in disjoint regions (anchors 100+ lines apart). Expect clean auto-merge. **If a hunk-adjacency conflict is flagged: keep BOTH groups** — disjoint properties, no semantic overlap. (Verified E2-2 tip `5b91780`; both groups are separate hunks.)

### H2. `src/ValleyAgent/Initialization/EventHandlerInitializer.cs`
- E2-2 (3 hunks): `ChatBoxInputPatch.Initialize` + `Chat.ChatBarRouter.Initialize` after `DialogueBoxInputPatch.Initialize` (base ~L239); `Chat.ChatBarRouter.Reset()` in `CleanupForTitleReturn` (base ~L377-381); `Chat.ChatBarRouter.ProcessPendingReplies()` after `Patches.DialogueBoxInputPatch.ProcessPendingReplies()` (base ~L1157).
- E2-3 (2 hunks): `SpeechDisplayRouter.ApplyConfig` in GMCM `OnConfigChanged` (base ~L170); `SpeechDisplayRouter.Tick()` after `_transcriptSink?.Drain()` (base ~L1152).
- **VERIFIED**: all 5 hunks have distinct anchors. The two `OnUpdateTicked` hunks are ~5 base lines apart and share ONE context line (`NPCGiftPatch.ProcessMainThreadActions();` — last context line of the E2-3 hunk, first of the E2-2 hunk). Git 3-way sliding window should apply both cleanly because each hunk's full context survives the other's insertion (E2-3 inserts ABOVE the shared line; E2-2 inserts BELOW it).
- **If conflict flagged: keep BOTH.** Final order in `OnUpdateTicked`:
  ```
  _transcriptSink?.Drain();
  // E2-3: Utils.SpeechDisplayRouter.Tick();        ← drains expired long-text chat messages
  ...
  NPCGiftPatch.ProcessMainThreadActions();
  Api.ValleyAgentApi.ProcessMainThreadActions();
  Patches.DialogueBoxInputPatch.ProcessPendingReplies();
  // E2-2: Chat.ChatBarRouter.ProcessPendingReplies();  ← renders chat-bar replies, same tick
  ProcessPendingMainThreadCommands();
  ```
  `Tick()` drains long-text chat messages; `ProcessPendingReplies()` renders chat-bar LLM replies. Independent queues; same-tick ordering is safe.

### H3. `src/ValleyAgent/Initialization/ServiceInitializer.cs`
- E2-3: `using ValleyAgent.Utils;` (after `using ValleyAgent.Commands;`, base L28) + `SpeechDisplayRouter.ApplyConfig/SetChatSink/SetBubbleSink` after DynamicSpeed config (base L68).
- E5-1: `using ValleyAgent.Services.Schedule;` (after `using ValleyAgent.Services;`, base L25) + `_container.RegisterSingleton(new NpcScheduleService())` near SaveDataManager registration (base L214).
- **VERIFIED**: usings at base L25 vs L28 (distinct lines in the same block → git applies both); registrations 146 lines apart. **Resolution: keep both usings + both registrations.** No using collision (`ValleyAgent.Services` vs `ValleyAgent.Services.Schedule` are distinct namespaces; `ValleyAgent.Utils` separate).

### H4. Cross-branch coupling (semantic — verified, no code change required)
- E2-3 rewrote `ActiveSpeechRouter.Route` body to `SpeechDisplayRouter.Route(npc, player, text)` but **kept public signature `public static void Route(NPC npc, Farmer player, string text)`**.
- E2-2 `Chat/ChatBarRouter.cs:171` calls `ActiveSpeechRouter.Route(npc, Game1.player, reply.Speech)` — exact match; ChatBarRouter already has `using ValleyAgent.Services;`.
- `SpeechDisplayRouter.Route(NPC, Farmer, string, int bubbleDurationMs = 3000, int bubbleDistanceThreshold = 8)` — the 3-arg call uses defaults. Compiles.
- **Resolution: none required** — no call-site update, no invented break. Post-merge behavior: chat-bar replies flow through E2-3's display pipeline (long text → split chat-bar with interval; ≤1 sentence → bubble ≤8 tiles / chat otherwise) — the intended composition.

## 3. File-overlap matrix (verified via `git diff --name-only`)

| | E2-2 | E2-3 | E5-1 |
|---|---|---|---|
| E2-2 | — | ModConfig.cs, EventHandlerInitializer.cs | ∅ |
| E2-3 | ModConfig.cs, EventHandlerInitializer.cs | — | ServiceInitializer.cs |
| E5-1 | ∅ | ServiceInitializer.cs | — |

## 4. Gates (run after EVERY merge commit, before proceeding)

1. `dotnet test src\ValleyAgent.UnitTests\ValleyAgent.UnitTests.csproj -p:GamePath="D:\Source\ValleyTalk\Stardew Valley"` — 0 fail.
2. Full build of all 6 projects (ValleyAgent, ValleyAgent.Abstractions, ValleyAgent.Autopilot, ValleyAgent.TestMod, ValleyTalk.ApiTest, ValleyAgent.UnitTests) — 0 errors 0 warnings.

**Baseline on `591f924` (established before merging)**: all 6 projects build with 0 errors; only 2 warnings — `CA2024` in ApiTest `KimiProvider.cs:270` and `OpenAICompatibleProvider.cs:150`. Pre-existing, OUT OF SCOPE (verify per gate, do not fix).

## 5. Commit messages (one `--no-ff` merge commit per branch)

- `merge: E2-3 into phase0 (long-text rules) [2026-08-03]`
- `merge: E2-2 into phase0 (chat-routing + session) [2026-08-03]`
- `merge: E5-1 into phase0 (schedule data) [2026-08-03]`

No `-X theirs/ours` anywhere — every conflict hunk resolved deliberately per above.
